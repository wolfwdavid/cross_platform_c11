[Area B review] Self-review of the local test runnability diff (PR pending):

- Sources/SocketControlSettings.swift: added `processIdentifier: pid_t = getpid()` parameter to `socketPath()` for test injectability. Made `fallback` mutable. After the `defaultSocketPath` call, intercept the case where (a) `XCTestConfigurationFilePath` is in env AND (b) fallback resolves to `/tmp/c11-debug.sock` — replace with `/tmp/c11-test-\(pid).sock`. Guard sits BEFORE the tagged-debug-path check, so when CMUX_TAG is set (via the scheme env block) the path is `/tmp/c11-debug-local-xctest.sock` and the guard becomes inert; when CMUX_TAG is absent (the historical footgun case) the guard activates. Both return-fallback sites pick up the rewritten value.
- Existing callers verified: 4 production callers in Sources/ use `SocketControlSettings.socketPath()` with no args → ProcessInfo env, no XCTest var in production → guard inert. 15+ existing test cases in c11Tests/SocketControlPasswordStoreTests.swift + c11Tests/GhosttyConfigTests.swift use explicit `environment: [...]` dicts with no XCTest var → guard inert. No behavior change for any existing call site.
- c11Tests/SocketControlPasswordStoreTests.swift: appended `SocketControlSettingsXCTestIsolationTests` (5 cases). Validator's grep should be `grep -rn 'XCTestConfigurationFilePath' c11Tests/` (NOT `c11LogicTests/` — there is no such directory on disk; c11LogicTests target sources live in c11Tests/ and are members via pbxproj). The new class lives in a file that's a member of c11LogicTests via pbxproj. Decision: appended a class rather than creating a new file to avoid the xcodeproj-gem-induced pbxproj diff bloat per CLAUDE.md.
- GhosttyTabs.xcodeproj/.../c11-unit.xcscheme: added `<EnvironmentVariables>` block to TestAction with `CMUX_TAG=local-xctest`. Belt-and-braces — survives if anyone wipes the runtime guard later. Schema's existing `shouldUseLaunchSchemeArgsEnv='YES'` means the env vars flow into the test host.
- scripts/test-unit-local.sh: new wrapper. Exports CMUX_TAG, CMUX_SOCKET_PATH, CMUX_ALLOW_SOCKET_OVERRIDE — three layers of defense matching the audit §2 fix surface A+B+D. Uses isolated derivedDataPath so it doesn't fight CI's build-test-local cache.
- CLAUDE.md: replaced the 'Do not run locally' prohibition + 'PR #164 incident' reference with positive guidance pointing at `scripts/test-unit-local.sh`. Validator's `grep -in \"don't run xcodebuild\" code/c11/CLAUDE.md` will now return nothing.
- Out of scope: did not touch SocketControlSettings semantics for any path other than the specific `/tmp/c11-debug.sock` → per-PID rewrite under XCTest. No submodule edits.